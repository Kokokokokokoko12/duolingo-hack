name: Probíhá lekce Duolinga XP farma - zadané číslo/input x 10, po dokončení se XP přidávají postupně

on:
  workflow_dispatch:
    inputs:
      lessons:
        default: 1
        description: 'Tady napiš počet lekcí!'
        required: false
        type: number

jobs:
  study:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - run: node index.js
        env:
          DUOLINGO_JWT: ${{ secrets.DUOLINGO_JWT }}
          LESSONS: ${{ inputs.lessons }}
